[Unit]
Description=Quiz Flask Server
After=network.target

[Service]
ExecStart=/opt/quiz-venv/bin/python /opt/quiz/run.py
WorkingDirectory=/opt/quiz
User=pi
Restart=always

[Install]
WantedBy=multi-user.target
